/**
 * Port interfaces for the Sale module application layer.
 * 
 * <p>Contains input ports (use case interfaces) that define the application's capabilities.
 */
package com.sigrap.sale.application.port;
