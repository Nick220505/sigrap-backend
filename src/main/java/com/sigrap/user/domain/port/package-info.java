/**
 * Repository port interfaces for the User module.
 * These interfaces define the contract for persistence operations without exposing implementation details.
 * Interfaces are defined in the domain layer and implemented in the infrastructure layer.
 */
package com.sigrap.user.domain.port;
