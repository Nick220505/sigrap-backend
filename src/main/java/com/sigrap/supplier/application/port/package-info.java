/**
 * Ports (interfaces) for the Supplier application layer.
 * Contains input ports (use case interfaces) and output ports (if needed beyond domain ports).
 */
package com.sigrap.supplier.application.port;
