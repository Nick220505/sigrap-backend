/**
 * Output adapters for the Supplier module.
 * 
 * <p>Output adapters (also called driven adapters) implement the ports defined by the
 * application/domain layers. They handle external dependencies like databases,
 * external APIs, message queues, etc.
 * 
 * <p>This package contains:
 * <ul>
 *   <li>Persistence adapters (JPA implementations)</li>
 *   <li>External service clients (if any)</li>
 * </ul>
 */
package com.sigrap.supplier.infrastructure.adapter.out;
