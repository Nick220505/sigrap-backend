/**
 * Output ports (repository interfaces) for the product module.
 * These interfaces define the contract for persistence operations
 * and are implemented in the infrastructure layer.
 * 
 * <p>Key interfaces:</p>
 * <ul>
 *   <li>{@link com.sigrap.product.domain.port.ProductRepositoryPort} - Product repository port</li>
 * </ul>
 */
package com.sigrap.product.domain.port;
